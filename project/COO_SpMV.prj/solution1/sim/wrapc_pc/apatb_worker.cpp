// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.2
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "dest"
#define AUTOTB_TVOUT_dest  "../tv/cdatafile/c.worker.autotvout_dest.dat"
#define AUTOTB_TVIN_dest  "../tv/cdatafile/c.worker.autotvin_dest.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "dest"
#define AUTOTB_TVOUT_PC_dest  "../tv/rtldatafile/rtl.worker.autotvout_dest.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			dest_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{dest " << dest_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int dest_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};


#define worker AESL_ORIG_DUT_worker
extern void worker (
float dest[100]);
#undef worker

void worker (
float dest[100])
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "dest"
		aesl_fh.read(AUTOTB_TVOUT_PC_dest, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_dest, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_dest, AESL_token); // data

			sc_bv<32> *dest_pc_buffer = new sc_bv<32>[100];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'dest', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'dest', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					dest_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_dest, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_dest))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: dest
				{
					// bitslice(31, 0)
					// {
						// celement: dest(31, 0)
						// {
							sc_lv<32>* dest_lv0_0_99_1 = new sc_lv<32>[100];
						// }
					// }

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: dest(31, 0)
						{
							// carray: (0) => (99) @ (1)
							for (int i_0 = 0; i_0 <= 99; i_0 += 1)
							{
								if (&(dest[0]) != NULL) // check the null address if the c port is array or others
								{
									dest_lv0_0_99_1[hls_map_index++].range(31, 0) = sc_bv<32>(dest_pc_buffer[hls_map_index].range(31, 0));
								}
							}
						}
					}

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: dest(31, 0)
						{
							// carray: (0) => (99) @ (1)
							for (int i_0 = 0; i_0 <= 99; i_0 += 1)
							{
								// sub                    : i_0
								// ori_name               : dest[i_0]
								// sub_1st_elem           : 0
								// ori_name_1st_elem      : dest[0]
								// output_left_conversion : *(int*)&dest[i_0]
								// output_type_conversion : (dest_lv0_0_99_1[hls_map_index++]).to_uint64()
								if (&(dest[0]) != NULL) // check the null address if the c port is array or others
								{
									*(int*)&dest[i_0] = (dest_lv0_0_99_1[hls_map_index++]).to_uint64();
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] dest_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "dest"
		char* tvin_dest = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_dest);
		char* tvout_dest = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_dest);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_dest, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_dest, tvin_dest);

		sc_bv<32>* dest_tvin_wrapc_buffer = new sc_bv<32>[100];

		// RTL Name: dest
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: dest(31, 0)
				{
					// carray: (0) => (99) @ (1)
					for (int i_0 = 0; i_0 <= 99; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : dest[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : dest[0]
						// regulate_c_name       : dest
						// input_type_conversion : *(int*)&dest[i_0]
						if (&(dest[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> dest_tmp_mem;
							dest_tmp_mem = *(int*)&dest[i_0];
							dest_tvin_wrapc_buffer[hls_map_index++].range(31, 0) = dest_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 100; i++)
		{
			sprintf(tvin_dest, "%s\n", (dest_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_dest, tvin_dest);
		}

		tcl_file.set_num(100, &tcl_file.dest_depth);
		sprintf(tvin_dest, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_dest, tvin_dest);

		// release memory allocation
		delete [] dest_tvin_wrapc_buffer;

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		AESL_ORIG_DUT_worker(dest);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_dest, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_dest, tvout_dest);

		sc_bv<32>* dest_tvout_wrapc_buffer = new sc_bv<32>[100];

		// RTL Name: dest
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: dest(31, 0)
				{
					// carray: (0) => (99) @ (1)
					for (int i_0 = 0; i_0 <= 99; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : dest[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : dest[0]
						// regulate_c_name       : dest
						// input_type_conversion : *(int*)&dest[i_0]
						if (&(dest[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> dest_tmp_mem;
							dest_tmp_mem = *(int*)&dest[i_0];
							dest_tvout_wrapc_buffer[hls_map_index++].range(31, 0) = dest_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 100; i++)
		{
			sprintf(tvout_dest, "%s\n", (dest_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_dest, tvout_dest);
		}

		tcl_file.set_num(100, &tcl_file.dest_depth);
		sprintf(tvout_dest, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_dest, tvout_dest);

		// release memory allocation
		delete [] dest_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "dest"
		delete [] tvout_dest;
		delete [] tvin_dest;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

